# Shadow-HGC-L LAD Stage Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add the opt-in Shadow-HGC-L / LAD stage with train-label-only label-affinity demand, a compiled target-demand MLP head, boundary-aware prototype support, no-diffusion promoted scripts, seed-42 experiments, diagnostics, and a final stage report.

**Architecture:** Keep Shadow-HGC-R-1 defaults frozen. LAD is an explicit stage path that reuses existing demand/prototype/shadow materialization, then optionally trains a compiled demand table head on prototype rows and evaluates identical-schema rows on original target nodes. Diffusion code remains in the repo but LAD scripts set `diffusion_enabled=false` and use only base / label_affinity / label_affinity_metapath feature modes.

**Tech Stack:** Python, PyTorch, sklearn MiniBatchKMeans fallback already used by the repo, local conda env `C:\Users\slian\anaconda3\envs\pytorch\python.exe`, pytest, CSV/JSON/Markdown experiment outputs.

---

### Task 1: Label-Affinity Demand Core

**Files:**
- Create: `shadow_hgc/features/label_affinity.py`
- Test: `tests/test_label_affinity_target_target.py`
- Test: `tests/test_label_affinity_non_target_leave_one_out.py`
- Test: `tests/test_label_affinity_uses_train_labels_only.py`
- Test: `tests/test_lad_outputs_no_nan.py`

- [ ] Write failing tests for target-target destination-row LAD, non-target leave-one-out, train-label-only invariance, and no-NaN block normalization.
- [ ] Implement train-label-only scatter histograms without dense edge-class tensors.
- [ ] Run the four LAD tests and then full pytest.

### Task 2: Compiled Demand Schema And MLP

**Files:**
- Create: `shadow_hgc/features/compiled_table.py`
- Create: `shadow_hgc/models/compiled_demand.py`
- Test: `tests/test_compiled_demand_schema.py`
- Test: `tests/test_compiled_demand_head_logits.py`
- Test: `tests/test_compiled_train_infer_schema_match.py`

- [ ] Write failing tests for schema order/dim stability, raw logits that can be negative, and train/infer schema identity.
- [ ] Implement `CompiledDemandBlock`, `CompiledDemandSchema`, row compilation, schema JSON serialization, and `CompiledDemandMLP` with concat and sum-logits fusions.
- [ ] Run compiled-head tests and then full pytest.

### Task 3: Boundary-Aware Prototype Helper

**Files:**
- Create: `shadow_hgc/prototype/boundary.py`
- Test: `tests/test_boundary_prototypes_budget.py`

- [ ] Write failing tests for budget split, per-class minimum, and diagnostics.
- [ ] Implement entropy/margin/loss scoring, class budget splitting, kmeans/kcenter center selection, final all-node assignment, and diagnostics.
- [ ] Run boundary tests and then full pytest.

### Task 4: Pipeline Integration

**Files:**
- Modify: `shadow_hgc/pipeline/core.py`
- Modify: `shadow_hgc/models/losses.py`
- Test: `tests/test_lad_no_diffusion_default.py`

- [ ] Add opt-in LAD arguments without changing default R-1 behavior.
- [ ] Build LAD blocks for relations using train labels only and normalize them.
- [ ] Build compiled prototype rows from self feature, reconstructed feature demand, LAD blocks, and degree block.
- [ ] Build identical-schema original target rows for inference.
- [ ] Train `CompiledDemandMLP` when `compiled_head=True`, log required diagnostics, and keep graph training as the default path.
- [ ] Add focal loss support where needed.

### Task 5: LAD Scripts And Experiment Matrix

**Files:**
- Create: `scripts/run_lad_stage.py`
- Create: `scripts/run_lad_small.py`
- Create: `scripts/run_lad_medium.py`
- Create: `scripts/run_lad_diagnostics.py`
- Modify: `scripts/run_small.py`
- Modify: `scripts/run_medium.py`

- [ ] Add CLI flags requested by the prompt.
- [ ] Ensure LAD scripts default to seed 42, diffusion disabled, no promoted diffusion feature mode.
- [ ] Run exactly V0 current_best, V1 compiled head, V2 compiled+LAD, V3 compiled+LAD+boundary for required datasets/ratios.
- [ ] Write required CSV/JSON outputs.

### Task 6: Experiments, Report, Push

**Files:**
- Create: `experiments/tables/lad_stage_small_seed42.csv`
- Create: `experiments/tables/lad_stage_medium_seed42.csv`
- Create: `experiments/tables/lad_stage_diagnostics_seed42.csv`
- Create: `experiments/reports/lad_stage_summary.md`

- [ ] Run pytest using the local conda env.
- [ ] Run LAD stage experiments with seed 42.
- [ ] Generate the detailed report with required sections and direct bottleneck answers.
- [ ] Verify no diffusion promoted rows, no `.mmap` staging, and no secrets.
- [ ] Commit and push to `origin/main`.
